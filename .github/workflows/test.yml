name: tests

on:
  push:
    branches: [main, reviewer-changes]
  pull_request:
    branches: [main]

permissions:
  contents: read
  id-token: write

jobs:
  test:
    name: ${{ matrix.os }} / py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        # macos-15 is Apple Silicon; macos-15-intel retains x86_64 coverage.
        os: [ubuntu-latest, windows-latest, macos-15, macos-15-intel]
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - uses: astral-sh/setup-uv@v6

      - run: uv sync --locked --extra dev

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Install the official BIDS Validator
        run: deno install -ERWN -g -n bids-validator jsr:@bids/validator@3.0.1

      - name: Run tests
        if: matrix.os != 'ubuntu-latest' || matrix.python-version != '3.11'
        run: uv run --locked pytest -vv -ra

      - name: Run tests with coverage
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        run: >-
          uv run --locked pytest -vv -ra
          --cov=pyxations
          --cov-report=term-missing
          --cov-report=xml
          --cov-fail-under=95

      - name: Upload coverage
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.xml
          use_oidc: true
          fail_ci_if_error: false
